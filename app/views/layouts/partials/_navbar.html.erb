	  <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            Menu
          </a>
          <a class="brand" href="/dashboard"><b>ThanxUp</b> Beta dashboard</a>
          <div class="nav-collapse collapse">
            <ul class="nav">
              <%- if current_user && current_user.role_access?(User.user_role) %>
                <li class="<%= active_tab('status') %>"><%= link_to t(:status, scope: :'myinfo.menu'), status_path %></li>
                <%- if current_user && current_user.role_access?(User.manager_role) %>
                  <li class="<%= active_tab('admin') %>"><%= link_to t(:admin, scope: :'myinfo.menu'), admin_path %></li>
                <%- end %>
              <%- end %>
              <li class="<%= active_tab('help') %>"><%= link_to t(:help, scope: :'myinfo.menu'), help_path %></li>
            </ul>
            <ul class="nav pull-right">
              <li>
              <p class="navbar-text">
              <% if user_signed_in? -%>
                <%= link_to image_tag(avatar_url(current_user), :size => "25x25"), fb_edit_user_path, :remote => true %>
                &nbsp;
                <%= link_to current_user.username, fb_edit_user_path, :remote => true %>&nbsp;|&nbsp; 
                <%= link_to t(:logout, scope: :'myinfo.menu'), destroy_user_session_path, :method=>'delete' %> 
              <% else -%>
                <%= link_to t(:login, scope: :'myinfo.menu'), new_user_session_path, :method=>'get'%>&nbsp;|&nbsp;
                <%= link_to t(:sign_up, scope: :'myinfo.menu'), fb_create_user_path, :remote => true %>
              <% end -%>
              </p>
              </li>
            </ul>      
          </div>
        </div>
      </div>
    </div>
